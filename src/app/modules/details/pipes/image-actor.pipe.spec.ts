import { ImageActorPipe } from './image-actor.pipe';

describe('ImageActorPipe', () => {
  it('create an instance', () => {
    const pipe = new ImageActorPipe();
    expect(pipe).toBeTruthy();
  });
});
