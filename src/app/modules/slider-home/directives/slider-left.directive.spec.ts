import { SliderLeftDirective } from './slider-left.directive';

describe('SliderLeftDirective', () => {
  it('should create an instance', () => {
    const directive = new SliderLeftDirective();
    expect(directive).toBeTruthy();
  });
});
