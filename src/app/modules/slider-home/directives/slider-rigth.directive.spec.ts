import { SliderRigthDirective } from './slider-rigth.directive';

describe('SliderRigthDirective', () => {
  it('should create an instance', () => {
    const directive = new SliderRigthDirective();
    expect(directive).toBeTruthy();
  });
});
