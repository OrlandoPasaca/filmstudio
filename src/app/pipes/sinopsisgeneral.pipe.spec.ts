import { SinopsisgeneralPipe } from './sinopsisgeneral.pipe';

describe('SinopsisgeneralPipe', () => {
  it('create an instance', () => {
    const pipe = new SinopsisgeneralPipe();
    expect(pipe).toBeTruthy();
  });
});
